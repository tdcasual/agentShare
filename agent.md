# Agent Notes

## Design Preference

- The user explicitly prefers the `kawaii` visual direction in this project.
- Treat `kawaii` as an intentional product requirement, not a defect.
- Do not suggest removing, toning down, or "correcting" the `kawaii` style unless the user explicitly asks for that change.
- Do not frame `kawaii` motifs, soft pastel palettes, playful icons, sparkles, flowers, hearts, or similar visual language as a problem by default.
- Future optimization work should preserve the `kawaii` direction while improving consistency, usability, accessibility, responsiveness, and implementation quality.

## Screenshot Baseline

- Page screenshots were captured on `2026-04-21`.
- Screenshot directory:
  `/Users/lvxiaoer/Documents/codeWork/agentShare/output/playwright/screenshots/2026-04-21-control-plane-pages`
- Screenshot manifest:
  `/Users/lvxiaoer/Documents/codeWork/agentShare/output/playwright/screenshots/2026-04-21-control-plane-pages/manifest.tsv`

## Capture Context

- The capture session was unauthenticated.
- Public demo and utility routes render directly.
- Most management routes currently present the login experience when accessed without a session.
