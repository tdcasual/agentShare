# Plans Directory Status

Files in this directory are execution plans, design snapshots, and historical change context.

They should **not** be treated as the primary source of truth for the current product framing unless a current guide points to them explicitly.

**Start here for the current architecture position:**
[`docs/guides/current-architecture-authority.md`](../guides/current-architecture-authority.md)

Additional reading guidance:

- older plans may preserve token-centered or migration-era language because they describe work at that point in time
- newer P3 plans assume the same `agent server first` application baseline and only extend the operating model around it
